# RabbitMQDemo1
Primeiro projeto publicando e consumindo uma fila no rabbitmq.
É utilizado um projeto web para o usuário escrever a mensagem, e uma webapi (poderia ser um projeto console também), para consumir as mensagens da fila e exibir no console do Visual Studio.
Foi utilizado docker e docker-compose para que o ambiente seja o mais simples de reproduzir.